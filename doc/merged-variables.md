# var-ready
`ready.sound.{player}` and `ready.{player}` were merged to form `{player}-once`.

DONE


# var-game
`door.sound.open` and `door.sound.close` were merged.

DONE

# var-fireworks
All `game.mode.ffa.win.fireworks.{player}` variables were merged to form `fireworks.ffa`.

DONE